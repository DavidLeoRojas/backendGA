# application.properties

# Server configuration
server.port=8080

# Spring Datasource (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/gemini_ambiental_DB?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=gemini_app
spring.datasource.password=G3m1n1@Amb13nt4l2025
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
# Importante: no altera el esquema manual
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Logging
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate=DEBUG